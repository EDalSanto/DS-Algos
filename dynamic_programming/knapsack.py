# input -> numbers that represent values of house [ 4, 1, 1, 4, 5, 6 ]
# output -> houses that should rob to get max value
# constraints -> can't rob consecutive houses

# 4 -> 1 -> 5

# Recursive backtracking(houses, to_rob=[])
# if length(houses) == 1 return houses[0]
# for each house in arr
    # if valid_house_to_rob(house)
        # add to to_rob
        #

